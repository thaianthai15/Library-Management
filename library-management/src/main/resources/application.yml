server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5432/library_db
    username: postgres
    password: Hoangoaingu1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
